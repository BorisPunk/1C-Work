&НаКлиенте
Процедура РежимПриИзменении(Элемент)
	
	Если Объект.Режим = ПредопределенноеЗначение("Перечисление.РежимыИБ.Файловый") Тогда
		ЭтаФорма.Элементы.Сервер.Видимость = Ложь;
		ЭтаФорма.Элементы.ИмяБазы.Видимость = Ложь;
		ЭтаФорма.Элементы.Каталог.Видимость = Истина;
	Иначе
		ЭтаФорма.Элементы.Сервер.Видимость = Истина;
		ЭтаФорма.Элементы.ИмяБазы.Видимость = Истина;
		ЭтаФорма.Элементы.Каталог.Видимость = Ложь;
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ЭтоКопияПриИзменении(Элемент)
	
	Если Объект.ЭтоКопия Тогда
		ЭтаФорма.Элементы.ОригинальнаяИБ.Видимость = Истина;
	Иначе
		ЭтаФорма.Элементы.ОригинальнаяИБ.Видимость = Ложь;
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ПриОткрытии(Отказ)
	
	Если Объект.Режим = ПредопределенноеЗначение("Перечисление.РежимыИБ.Файловый") Тогда
		ЭтаФорма.Элементы.Сервер.Видимость = Ложь;
		ЭтаФорма.Элементы.ИмяБазы.Видимость = Ложь;
		ЭтаФорма.Элементы.Каталог.Видимость = Истина;
	ИначеЕсли Объект.Режим = ПредопределенноеЗначение("Перечисление.РежимыИБ.Серверный") Тогда
		ЭтаФорма.Элементы.Сервер.Видимость = Истина;
		ЭтаФорма.Элементы.ИмяБазы.Видимость = Истина;
		ЭтаФорма.Элементы.Каталог.Видимость = Ложь;
	Иначе
		ЭтаФорма.Элементы.Сервер.Видимость = Ложь;
		ЭтаФорма.Элементы.ИмяБазы.Видимость = Ложь;
		ЭтаФорма.Элементы.Каталог.Видимость = Ложь;
	КонецЕсли;
	
КонецПроцедуры
